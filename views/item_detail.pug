extends layout 

block content 
  h1= title
  p= item.description
  p= item.price_formatted
  p= item.stock
  if item.stock <= 25
    p= `Only ${item.stock} left!`
  p= item.totalVal
  div 
    a(href=`${item._id}/update`) Update item
  div 
    a(href=`${item._id}/delete`) Delete item